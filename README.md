Project Procedure:

1. The program starts and displays the application name and version.

2. The user enters a string through the console.

3. The input string is processed (converted to lowercase and special characters removed if required).

4. The program checks whether the string is a palindrome.